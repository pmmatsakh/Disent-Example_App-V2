# openai_codex wrapper. On the container: `uv pip install -r requirements.txt`.
# The codex CLI: `npm install @openai/codex` (local, in the example folder).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
